{
  "name": "@repo/dev-tools",
  "private": true,
  "scripts": {
    "db-down": "docker compose --project-directory ../.. -f ../../compose.yaml down",
    "db-logs": "docker compose --project-directory ../.. -f ../../compose.yaml logs -f postgres",
    "db-reset": "docker compose --project-directory ../.. -f ../../compose.yaml down -v && docker compose --project-directory ../.. -f ../../compose.yaml up -d postgres",
    "db-up": "./scripts/postgres.sh up",
    "dev": "./scripts/postgres.sh dev"
  }
}
